apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unifi-data
  labels:
    app: unifi-controller
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
